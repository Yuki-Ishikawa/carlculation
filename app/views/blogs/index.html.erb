<div class="image">
	<%= image_tag("/images/car2.jpg", :class => "image-size-top") %>
</div>

<div class="blog-product">
	<h3>Blogs list</h3>
		<div class="blog-item">
			<% @blogs.each do |blog| %>
				<div class="blog-item-contents clearfix">
					<div class="blog-item-left">
						<%= link_to blog_path(blog.id) do %>
							<%= attachment_image_tag blog, :image, fallback: "/images/no_image_photo.jpeg", :class => "blog-item-image" %>
						<% end %>
					</div>
					<div class="blog-item-right">
						<p>Title: <%= blog.title %></p>
						<p><%= truncate(blog.introduction, length:100) %></p>
						<%= link_to user_path(blog.user.id) do %>
							From: <%= blog.user.user_name %>
						<% end %>
						<div class="date">
							<span>Date: <%= blog.created_at.strftime('%Y/%m/%d') %></span>
						</div>
					</div>
				</div>
			<% end %>
		</div>
	<%= paginate @blogs %>
</div>
